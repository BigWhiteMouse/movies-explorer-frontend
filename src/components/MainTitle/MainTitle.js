import React from 'react';

function MainTitle({titleName}) {
  return (
    <div className="main-title">
      <h2 className="main-title__title">{titleName}</h2>
    </div>
  )
}

export default MainTitle;
